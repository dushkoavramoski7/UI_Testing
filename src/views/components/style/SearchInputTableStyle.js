export const searchInputTableStyle = (theme) => {
    return {
        cursorPointerHover: {
            ...theme.common?.cursorPointerHover
        },
        greyHover: {
            ...theme.common?.greyHover
        }
    }
}