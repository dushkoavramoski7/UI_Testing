export const loginViewStyle = (theme) => {
    return {
        inputField: {
            ...theme.common.inputField
        },
        errorText: {
            ...theme.common.errorText
        },
    }
}