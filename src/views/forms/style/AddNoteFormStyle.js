export const addNoteFormStyle = (theme) => {
    return {
        errorText: {
            ...theme.common?.errorText
        },
        inputField: {
            ...theme.common?.inputField
        },
    }
}